# Backend base URL the browser may see for WS / absolute links.
# Server Components use API_INTERNAL_URL + API_KEY (server-only) instead.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Server-only (never prefix with NEXT_PUBLIC_)
# API_KEY=
# API_INTERNAL_URL=http://localhost:8000
